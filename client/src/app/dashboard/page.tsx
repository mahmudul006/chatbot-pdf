import { Chat } from 'src/sections/one/chat';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Dashboard: One',
};

export default function Page() {
  // return <OneView />;
  return <Chat />;
}
